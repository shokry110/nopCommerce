services:
    sarayetel_web:
        build: .
        container_name: sarayetel
        ports:
            - "80:80"
        depends_on:
            - sarayetel_database
    sarayetel_database:
        image: "mysql:latest"
        container_name: sarayetel_mysql_server
        restart: "always"
        environment:
          MYSQL_ROOT_PASSWORD: "sarayetel_db_password"

volumes:
  sarayetel_data:            